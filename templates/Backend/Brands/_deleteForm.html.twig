<form action="{{path ('app.admin.brands.delete', {id: brand.id}) }}" method='POST' onsubmit="return confirm ('Etes vous sûr de vouloir supprimer ce gender?')">
	<input type="hidden" name="token" value="{{ csrf_token('delete' ~ brand.id)}}">
	<button type="submit">
		Supprimer</button>
</form>
