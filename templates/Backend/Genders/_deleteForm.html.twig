<form action="{{path ('app.admin.genders.delete', {id: gender.id}) }}" method='POST' onsubmit="return confirm ('Etes vous sûr de vouloir supprimer ce gender?')">
<input type="hidden" name="token" value="{{ csrf_token('delete' ~ gender.id)}}">
<button type="submit"> Supprimer</button>
</form>
