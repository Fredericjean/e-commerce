<form action="{{path ('app.admin.taxes.delete', {id: taxe.id}) }}" method='POST' onsubmit="return confirm ('Etes vous sûr de vouloir supprimer ce gender?')">
<input type="hidden" name="token" value="{{ csrf_token('delete' ~ taxe.id)}}">
<button type="submit"> Supprimer</button>
</form>
